[tools]

# ---------- base ----------
just = "1.45"
jq = "1.8"
yq = "4.50"
sops = "3.11"
age = "1.3"
trivy = "0.68"

# ---------- k8s ----------
kubectl = "1.35"
helm = "4.0"

# ---------- iac ----------
python = "3.12"
pipx = "1.8"
terraform = "1.14"
ansible = "13.2"
sst = "3.17"

# ---------- docker ----------
docker-cli = "29.1"
docker-compose = "5.0"
docker-slim = "1.41"

# ---------- dev ----------
uv = "0.9"
bun = "1.3"
go = "1.25"
rust = "1.92"

